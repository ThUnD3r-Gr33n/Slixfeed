#!/usr/bin/env python3
# -*- coding: utf-8 -*-

"""

TODO

Move code  from sqlite.get_entry_unread

if num > 1:
    news_list += (
        "\n{}\n{}\n{}\n"
        ).format(str(title), str(link), str(feed_title))
else:
    news_list = (
        "{}\n{}\n{}"
        ).format(str(title), str(link), str(feed_title))

"""
